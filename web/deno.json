{
  "name": "wyn-playground",
  "version": "0.1.0",
  "tasks": {
    "dev": "deno run --allow-net --allow-read server.ts",
    "build-wasm": "cd ../wyn-wasm && wasm-pack build --target web --out-dir ../web/static/pkg"
  }
}
